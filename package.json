{
    "name": "social-media-app",
    "version": "1.0.0",
    "private": true,
    "description": "Full-stack social media app with FastAPI + React",
    "scripts": {
        "start": "npm run start:backend & npm run start:frontend",
        "start:backend": "cd SQL && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
        "start:frontend": "cd UI && npm run dev",
        "install:all": "npm install && cd SQL && pip install -r requirements.txt && cd ../UI && npm install"
    },
    "devDependencies": {
        "@testing-library/jest-dom": "^6.9.1",
        "@testing-library/react": "^16.3.2",
        "@testing-library/user-event": "^14.6.1",
        "@vitest/coverage-v8": "^4.1.0",
        "concurrently": "^8.2.2",
        "jsdom": "^29.0.0",
        "vitest": "^4.1.0"
    }
}
